import React from 'react';

const DeleteModel = () => {

  const buttonHandle = () => {
    
  };

  return (
    <div className="navButton" onClick={buttonHandle}>
      SUPPRIMER
    </div>
  );
}

export default DeleteModel;
